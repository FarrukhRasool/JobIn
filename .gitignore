# macOS
.DS_Store

# Generated output. Source is cv.typ and letter.md, both tracked.
# Regenerate with ./scripts/render-cv.sh <slug> and ./scripts/render-letter.sh <slug>
applications/*/*.pdf

# The Job-Hunting Obsidian vault lives at ~/Desktop/Job-Hunting, deliberately
# outside this repo. It holds personal job-search material and must not travel
# with the code. scripts/check-docs.py validates it against this repo.
