# Application notes

## Track
**ios-developer.** Score 80/100, standard tailoring. The posting has no AI or ML content anywhere in its verbatim requirements or responsibilities, so the ai-engineer track was never a candidate. No blending.

## Location claimed
**Berlin, Germany**, the job's own listed city, per the standing rule for European roles. (The role is 100% remote, but Berlin is the stated location, so it takes priority over the "no city named" fallback.)

## Email used
Master default, `farrukhrasool65@gmail.com`. Not overridden.

## Employment status shown
**Verimi: plain `iOS Developer`, no Working Student qualifier.** This is an ordinary professional interim posting, not a student, Werkstudent, internship or dual-study listing, so the qualifier stays off per the rule.

## No company research brief for this slug, and that is deliberate
Michael Page is a recruitment agency and the end client is not named anywhere in the posting. There is nothing to research and no company-specific vocabulary to mirror. The CV was built from the posting's own verbatim requirements block and the profile sources only.

## Led with
- **Verimi Swift Concurrency**, leading the Verimi section: the Wallet's login and registration flow built end to end on `async/await`, framed against the posting's own umbrella term "Swift Concurrency" rather than answering with `async/await` alone.
- **The PakWheels Combine advanced search**, leading the PakWheels section and the strongest single match on the whole CV: 18 `@Published` filter dimensions bound to a staged filter model with derived chips, immediately followed by a dedicated bullet on the `dropFirst()` hydration-loop fix. This answers the posting's own phrase "reaktive und deklarative Benutzeroberflächen mit SwiftUI und Combine" almost word for word.
- **The marketplace nature of PakWheels** stays visible through the fixed skeleton blurb ("Pakistan's biggest marketplace for buying and selling cars covering millions of live users"), which answers the optional "E-Commerce- oder Marktplatzumfeld" line without needing a dedicated bullet.
- **Code reviews, refactoring and mentoring** for the "Fokus auf Clean Code und hohe Qualitätsstandards durch Code-Reviews" responsibility line, stated as its own PakWheels bullet.
- **CI/CD ownership on both sides**: the Verimi xcframework automation (a number, TestFlight build time halved) and the PakWheels Jenkins/Fastlane `PRBuild` pipeline (a mechanism with a user). GitLab itself is not claimed in any bullet, since no evidence ties Farrukh's CI/CD work to that specific vendor. It is listed once in the sidebar as `GitLab CI/CD`, confirmed hands-on by Farrukh on 2026-08-03, and the transferable-vendor case rests on the Jenkins and Verimi CI stories rather than a fabricated GitLab bullet.
- **UIKit to SwiftUI migration across two apps at Verimi**, then the Storyboards through programmatic UIKit to SwiftUI arc at PakWheels, both directly evidencing "reaktive und deklarative Benutzeroberflächen mit SwiftUI" as a practised, repeated skill rather than a one-off.

## Experience balance
**Verimi: 5 bullets. PakWheels: 6 bullets. 11 total**, the upper end of the usual 9 to 11 range. PakWheels got the fuller section because it holds the single strongest posting match (Combine) plus the CI/CD and Clean Code responsibility answers, and because the source material supports six distinct, non-overlapping claims without padding. Both sections carry direct posting matches first, then adjacent senior signals (session/auth architecture, mentoring, legacy migration) rather than stopping at the exact keyword count.

## Cut
- **PakWheels crash-free score (above 99%), 20% launch time reduction, and the 40% SPM build-time improvement.** All three are strong, numbered evidence but none is named in the posting's own requirements text, and six PakWheels bullets already covered Combine, the hydration fix, session architecture, code review/mentoring, CI/CD and the migration arc. First candidates to add back if the page underfills.
- **PakWheels Core Data / car generation filter, A/B testing and feature flags, hiring and university job-fair interviewing, Scrum/Kanban.** Real and strong but none maps to anything in the posting's verbatim text, and the six bullets used were a closer fit.
- **Verimi SDD automation across Claude and Codex, and the Verimi Design Component SDK.** Kept the AI tooling to the sidebar only (`Claude | Codex`, `Spec-Driven Development (SDD)`) rather than an experience bullet, since the posting has zero AI content and a bullet on it would be padding, not relevance. The Design Component SDK was cut in favour of the identity SDK bullet, which is the stronger platform-ownership story and avoids naming two SDKs in one section.
- **Verimi certificate pinning (BARMER).** Real, but the posting never mentions secure transport, and the sidebar and bullets were already tight on relevant content.
- **Education: Deep Vision, Scene Detection and Agent-Based Modeling bullets, and the TensorFlow/PyTorch variant.** The posting has no ML content, so the M.Sc. section was kept to 3 bullets (Agentic AI, Embedded Intelligence, Autonomous Robot) rather than the full 5, per the track's own "one or two sentences, not a section" guidance for non-AI postings.
- **Bachelors bullets.** None of the four stack-specific bullets (MERN, Android/Java, .NET, C++) match anything the posting names, so the section stays empty as the default.

## Terminology mirrored
In main-column bullets: `Swift`, `SwiftUI`, `Combine`, `Swift Concurrency` (the umbrella term used in place of async/await alone, per skills.md's own instruction), `Code Reviews`. The PakWheels blurb's "marketplace" wording answers the posting's own "Marktplatzumfeld" phrase.

Sidebar only, no bullet behind them: Swift Testing, XCTest, GitLab CI/CD, Clean Code Architecture. See "Gaps not closed" below.

## Metrics worth adding
- **The PakWheels Jenkins/Fastlane `PRBuild` pipeline bullet has no time or effort number.** The mechanism (QR code, PR, Jira ticket) is concrete, but there is no figure for how much manual review or release time it actually saved per pull request, or how many PRs a month ran through it. A number here would move this from a strong mechanism bullet to a strong outcome bullet, matching the Verimi CI story's "halved" figure.
- **The code review and mentoring bullet has no outcome attached.** "Kept the codebase clean" and "mentored one junior developer" are both true but neither is quantified. If Farrukh has a real figure (review turnaround time, defect reduction, time to the mentee's first solo PR), it would strengthen the bullet that most directly answers the posting's own "hohe Qualitätsstandards durch Code-Reviews" line.
- **The Verimi identity SDK bullet quantifies clients (two insurers) but nothing else.** A number of endpoints, integration timeline, or scale of usage inside AOK or BARMER would sharpen the platform-ownership claim further.

## Gaps not closed
- **Swift Testing and GitLab CI/CD sit on the sidebar with no bullet behind either.** Both are `listed` in skills.md, confirmed hands-on by Farrukh on 2026-08-03, but genuinely unevidenced on the CV itself. Be ready to name a specific place each was used if asked at screening or interview.
- **No named end client.** Michael Page is a recruitment agency, so nothing about the actual employer could be researched or mirrored. This will surface at a first call, not before.
- **Interim contract, 4 to 6 months.** Not addressed anywhere in the CV, which is normal, but worth having an answer ready given "immediate start" and "no notice period" line up well with Farrukh's actual availability.
- **German-language posting, English-only CV.** Correct per the posting's own text ("Sehr gute Englischkenntnisse; Deutschkenntnisse von Vorteil"), but the score record flags a German-written advert as a soft signal of a German-speaking workplace regardless of what the text says. Not addressed in the CV, which is the right place to leave it.
- **"Foundation Framework" is not a named skill anywhere in `profile/skills.md`.** It is not on the CV as its own line, since inventing a skill entry that is not on the pool page is against the rule even though it is a routine and highly likely-true competency for someone building on UIKit, SwiftUI and Combine for four years. Genuinely absent from the profile's written record, not withheld by choice.

## Page count
**Not rendered.** This agent has no shell. Expect the render pass to run `./scripts/render-cv.sh michael-page-senior-ios-developer` next. My estimate: close to full, possibly a touch over one page given six PakWheels bullets (two of them, the session/auth bullet and the Jenkins/Fastlane bullet, run long).

**Cut order if it overflows in the main column** (diagnose the column first per the skill, do not assume): 1) drop the Autonomous Robot M.Sc. bullet, taking the section to 2; 2) fold the `dropFirst()` PakWheels bullet back into the Combine bullet as a single longer sentence, or cut it outright, since it is the one PakWheels bullet added on top of the six-bullet target for extra depth rather than for a distinct posting requirement; 3) cut the code review/mentoring PakWheels bullet, the least uniquely evidenced of the six.

**Add-back order if it underfills:** 1) PakWheels crash-free score and 20% launch time bullet, the strongest quality-standard evidence held in reserve; 2) the 40% SPM build-time improvement bullet, pairing with the CI/CD story already on the page; 3) a fourth M.Sc. bullet (Deep Vision, the CNN/ViT comparison); 4) only as a last resort, a Bachelors bullet, though none matches this posting's stack so this should not be needed.
