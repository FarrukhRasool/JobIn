# Application notes

**Track:** ai-engineer, as set by `/score`. Not re-decided here. The posting's own stack signal (GitHub fork suggests Flutter, not native iOS) confirms this isn't a blended role, so no `PRIORITY-both` angle to build in.

**Location claimed:** Cologne, Germany. Reasoning: the job record's location field says "Germany (remote, company referenced to Frankfurt)," but `jobs/research/dexter-health-ai-engineer.md` explicitly debunks the Frankfurt reference as a LinkedIn metro-area artefact with no real company connection, and warns it "should not be relied on for any relocation or timezone assumption." Putting Frankfurt on the CV would suggest a false local tie to a city the company has no real presence in. dexter health GmbH's verified registered address is Cologne (HRB 112599), so I treated this as the "remote, no [reliable] city named" branch of the rule and used the company's real HQ city. Flagging this in case Farrukh prefers to just mirror the literal LinkedIn field instead.

**Led with:**
- **Verimi bullet, moved to the top of that entry and reworded** to name the specific tools: "Automated Spec-Driven Development (SDD) for iOS across multiple Large Language Models (LLMs), such as Claude and Codex, to speed up feature delivery." This is the concrete evidence behind the posting's must-have "serious daily experience with Claude Code, Codex, Cursor, Copilot or similar" and it's production evidence, not a claimed skill.
- **Bedtime Story Teller, via the "Agentic AI" Masters bullet, moved to the top of that entry and rewritten** to name what the posting actually asks for: "Model Context Protocol (MCP) server running a self-hosted, open-source LLM (Ollama, Mistral), with an LLM planning step and deterministic fallback logic." This maps directly onto the responsibility line "design and implement LLM-based workflows with structured outputs, validation logic, and fallback behavior."
- Note: I found that `profile/projects.md` documents local Ollama/Mistral inference for this project ("nothing leaves the machine"), which is genuine self-hosted, open-source LLM evidence. I used it, since it traces cleanly to the source. This partially cuts against the "no self-hosted LLM experience" framing in the task brief. See Gaps below for why this is only a partial close, not a full one.

**Cut / downplayed (not removed, deprioritised):**
- Technical Skills sidebar: compressed "Swift | Objective-C", "SwiftUI | UIKit", "SDKs | SPM | Cocoa Pod" and "Reactive Programming | Combine" down to a single "Swift | SwiftUI" line at the bottom, per the ai-engineer track's explicit instruction to keep iOS to one line. The detail still exists in the Verimi/PakWheels bullets, just not repeated in the skills list.
- Within both Professional Experience entries, UI-specific bullets (SwiftUI/UIKit feature building, Design Component Library, Verimi Wallet UIKit-to-SwiftUI migration) were moved to the bottom of their entries. Nothing was deleted.
- Bachelor's degree bullets (MERN stack, Android calculator, .NET POS, C++ maze game) kept as-is and unreordered. None are relevant to this posting and none is stronger than another, so no reordering rationale applied.

**Bullets actually removed to hold one page** (done at render time, after the first draft rendered at two pages):

The tailored draft kept the master's exact bullet counts (6 / 6 / 6 / 4), so nothing was added. It overflowed because the bullets that matter most here were *lengthened*: the Verimi multi-LLM SDD line, the Agentic AI MCP line, and the SLAM line. Four bullets were cut to buy that space back, chosen as the weakest on the page for an LLM engineering role:

- Bachelor's: "Scientific Calculator : Android Mobile application using JAVA.", "Point of Sale : Developed in .NET, using WPF.", "Maze Runner Game : Developed in C++ using MFC." Undergraduate toy projects with no bearing on the posting. "Online Hotel Reservation System : MERN stack." was kept, as MERN is the only Bachelor's item touching the posting's frontend/fullstack nice-to-have.
- PakWheels: "Worked within Agile frameworks *Scrum* and *Kanban* in iterative sprints." Generic filler, and the sidebar already lists "Scrum | Kanban", so it was redundant as well as weak.

No Masters bullets were cut. That section carries the entire AI case for this application.

**Terminology mirrored from the posting and research brief:**
Python (via the Agentic AI and Autonomous Robot bullets, sourced from `projects.md`), Large Language Models / LLM, Model Context Protocol (MCP), fallback logic, self-hosted, open-source LLM, Claude, Codex, CI/CD, deployment process, Spec-Driven Development (SDD). Abbreviations spelled out on first use per the ATS rule: Large Language Models (LLMs), Model Context Protocol (MCP), Spec-Driven Development (SDD), Simultaneous Localisation and Mapping (SLAM).

**Metrics worth adding:**
- The lead Verimi bullet ("to speed up feature delivery") has no figure. If Farrukh has any sense of cycle time before/after, or the number of features or PRs the SDD workflow touched, that would turn the CV's single strongest keyword-match bullet into a quantified one.
- The Verimi CI/CD bullet ("reducing build times") also carries no number, unlike the PakWheels CocoaPods-to-SPM bullet which has the one real figure in the whole CV (40%). A percentage or absolute time saved here would help.
- No metric exists anywhere for AI output quality, evaluation pass rates, or latency for the Bedtime Story Teller or RosBot projects. Given the posting explicitly asks about "evaluation loops" and "optimizing latency, cost, quality," even an approximate figure (inference latency, number of test cases in the "real test suite," RosBot's particle count runtime) would help future applications to similar postings.

**Gaps not closed (stated plainly, per the task brief and my own check against the master and project bank):**
- **No production Python or backend experience.** Every professional (paid) role on the CV is Swift/iOS. Python/FastAPI only appears through the Bedtime Story Teller and RosBot entries, both academic/personal projects under Education, not Professional Experience. The CV is honest about this by placement (skills list vs. where the work actually sits), but the gap is real and a recruiter will see it immediately: the must-have is "strong software engineering skills, particularly in Python," and the shipped record is Swift.
- **No evaluation/validation pipeline work for AI output quality outside coursework.** The Bedtime Story Teller project has a software test suite (api/services/integration) and a deterministic fallback/decision policy for routing, which is adjacent to "validation logic" but is not the same as evaluating generated content quality. RosBot's own honest-limitations note in `projects.md` states directly: "No automated evaluation harness, so results are qualitative." I did not paper over this.
- **Self-hosted LLM experience: partially closed, not fully.** Ollama running Mistral locally for Bedtime Story Teller is genuine self-hosted, open-source LLM use, and I put it in the CV. But it's inference on a personal machine for a class project, not production model serving, no inference optimisation, no latency/cost tuning, no scaling infrastructure. The posting's nice-to-have language ("model serving, inference optimization experience") goes further than this evidence supports, so I did not claim more than the single local-inference fact.
- **No healthcare or regulated-industry background.** Nothing in `master-cv.typ` or `projects.md` touches healthcare, care documentation, or a regulated environment. Not addressed in the CV; would need to be addressed honestly in the letter, if at all, rather than implied here.

**TODO:VERIFY check:** I grepped `profile/master-cv.typ` and `profile/projects.md` for `TODO:VERIFY` and found none. The only `TODO` markers in `profile/projects.md` are undeveloped-project notes (Deep Vision, Scene Detect, Indoor Navigation, SwiftUI packages, Dolidroid, TodoPharma, MyPropertyFinder), none of which I used beyond the entries the master CV already carries. Employment dates, job titles, the Masters' "present" status, and the German (A2) / English (C1) language levels all come from `master-cv.typ`, which its own header states was verified against Farrukh's PDF resume on 2026-07-28, with no `TODO:VERIFY` tag anywhere in the file. Nothing was blocked or worked around.

**Page count:** one page, confirmed. Rendered to `Farrukh_Rasool_CV.pdf` via `./scripts/render-cv.sh dexter-health-ai-engineer`. First render was two pages, resolved by the four cuts listed above rather than by shrinking the template.

**Defect found in the master, not fixed here:** the role title renders as "IOS Developer" on both experience entries. Correct form is "iOS". This comes from `profile/master-cv.typ` lines 58 and 72, so it affects every tailored CV, not just this one. Not corrected in this copy, because the master is the single source of truth and generated CVs are never hand-edited for facts. Needs a one-word fix in the master, then a regenerate.

**Overall read on this application, stated directly:** This is a genuine long shot, matching the 57/100 "Maybe" score. The two lead bullets (Verimi SDD/multi-LLM, Bedtime Story Teller MCP/fallback/self-hosted LLM) are real, specific, and map cleanly onto the posting's most distinctive must-have and responsibility lines. But the core ask, production Python engineering shipping AI features into a live backend, is not on this CV anywhere as paid work, and that gap is visible on a single read-through, not just on close inspection.
